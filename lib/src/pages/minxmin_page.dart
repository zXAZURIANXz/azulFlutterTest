import 'package:flutter/material.dart';



class MinxMinPage extends StatelessWidget {
  const MinxMinPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('minuto por minuto'),
    );
  }
}